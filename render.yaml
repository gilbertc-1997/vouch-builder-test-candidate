services:
  - type: web
    name: vouch-handover
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: TRANSFORMERS_CACHE
        value: /app/.cache
      - key: SKIP_TRANSLATION
        value: "true"
